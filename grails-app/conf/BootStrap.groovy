import stackoverflow.*

class BootStrap {

    def init = { servletContext ->
      // (1..100).each {
      //   new Programador().save(failOnError: true)
      // }
    }
    def destroy = {
    }
}
